package com.example.employepoc.command.rest.dto;


/**
 * Defines the types of entities that can be involved in commands and events within the system.
 * This enumeration is used to distinguish between different entity types, such as PERSON and GROUP,
 * allowing for more specific handling in command and event processing.
 */
public enum Gender {
    MALE,
    FEMALE
}
